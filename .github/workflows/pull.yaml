on:
  pull_request:
    branches:
      - main

jobs:
  run:
    runs-on: ubuntu-latest 

    steps:
       - name: checkout code
         uses: actions/checkout@v3

       - name: echo command
         run: echo "Hello Github Actions!"
